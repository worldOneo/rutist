// Fix me
module((export) {
  export("New", class((def) {
    def("__init__", (self) {
      self._map = Map()
    })

    def("__setmember__", (self, key, value) {
      self._map.set(key, value)
    })

    def("__getmember__", (self, key) {
      self._map.get(key)
    })
  }))
})